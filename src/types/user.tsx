export type userServer = {
  id: number;
  email: string;
  first_name: string;
  last_name: string;
  avatar: string;
};

export type user = {
  id: number;
  email: string;
  firstName: string;
  lastName: string;
  avatar: string;
};
